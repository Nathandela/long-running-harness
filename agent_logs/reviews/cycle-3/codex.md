REVIEW_CHANGES_REQUESTED

- **[P1] Import rollback still leaves orphaned metadata when failure happens after `putMeta()` succeeds** ([media-pool-manager.ts:46](/Users/Nathan/Documents/Code/long-running-harness/src/audio/media-pool/media-pool-manager.ts:46), [media-pool-manager.ts:59](/Users/Nathan/Documents/Code/long-running-harness/src/audio/media-pool/media-pool-manager.ts:59))  
  The new catch block only deletes the blob. If `putMeta()` succeeds and `putPeaks()` or `computeWaveformPeaks()` fails, IndexedDB keeps the metadata record, so the next `init()` will resurrect a source entry whose blob was rolled back and whose peaks may be missing. Roll back `meta` as well, and ideally `deletePeaksBySource()` too for symmetry. Add a regression test that forces a failure after `putMeta()` and verifies a fresh pool does not load a ghost source.